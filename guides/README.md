# Guides

This directory holds reader-facing explanations of delivered behavior.

Name each file `<slug>.md`, using a unique kebab-case slug.
Required frontmatter is `id: guide.<slug>`, `title`, and one or more `describes` proposal IDs.
Guides have no `status` lifecycle; revise them when the behavior they explain changes.
History is preserved in the record: superseded proposals, policies, and decisions are never deleted — they are marked and linked.
