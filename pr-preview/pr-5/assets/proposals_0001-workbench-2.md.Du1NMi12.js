import{c as t,W as a,j as o,n as s}from"./chunks/framework.DBclLOWj.js";const u=JSON.parse('{"title":"Workbench 2.0","description":"","frontmatter":{"id":"proposal.0001","title":"Workbench 2.0","authors":["markmals","Claude"],"status":"implemented","pull-request":"https://github.com/markmals/workbench/pull/1","issues":[],"supersedes":[]},"headers":[],"relativePath":"proposals/0001-workbench-2.md","filePath":"proposals/0001-workbench-2.md"}'),i={name:"proposals/0001-workbench-2.md"};function r(n,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="workbench-2-0" tabindex="-1">Workbench 2.0 <a class="header-anchor" href="#workbench-2-0" aria-label="Permalink to “Workbench 2.0”">​</a></h1><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>Replaces Workbench&#39;s spec-driven multiplatform application template with a platform-agnostic, harness-agnostic process kit: vision, policies, decisions, proposals, guides, and previews, enforced by a record validator and continuous integration. Deletes the stack; keeps the process.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>Workbench 1.0 was a spec-driven multiplatform application template. It carried nine platform skills, four device-driver skills, eleven per-platform task scaffolds, a 42 KB toolchain catalog, reverse-pointer and drift-detection machinery, and a <code>/sdd-*</code> command surface — all in service of fanning one specification out across web, iOS, Android, Windows, Linux, a CLI, and a Convex backend.</p><p>That answered a question about <em>how to build the same product several times</em>. The question that actually matters is <em>how to keep an agent honest about any one change</em>, and 1.0 buried it. The process was the small part of a large repository about stacks.</p><p>Three specific failures made this worth rewriting rather than extending.</p><p><strong>The process had no durable workspace.</strong> Work happened on whatever branch was checked out. Three separate documents stated the repository did not use feature branches, and commit discipline prohibited pushing. There was nowhere for a proposal, its implementation, review discussion, and a status report to accumulate as one reviewable unit.</p><p><strong>Documentation was not an artifact.</strong> The repository rendered its own internal specifications and produced nothing a user would read. Behavior therefore had two representations — specification and code — where the process depends on having more than two, written independently, so they can disagree.</p><p><strong>Nothing was verifiable outside the agent&#39;s own session.</strong> There was no continuous integration, no release path, and no preview. Every quality claim rested on the agent reporting that it had run something.</p><p>The workaround available today is to ignore most of the repository and use the four process skills by hand. That works, and it is evidence that the process was the valuable part all along.</p><h2 id="proposed-solution" tabindex="-1">Proposed solution <a class="header-anchor" href="#proposed-solution" aria-label="Permalink to “Proposed solution”">​</a></h2><p>Delete the stack. Keep the process, and make the parts a machine can check into checks.</p><p>Cloning Workbench gives a project the structure described in <code>PROCESS.md</code> and nothing about a technology. The artifacts are a living <code>VISION.md</code>, plus <code>proposals/</code>, <code>policies/</code>, <code>decisions/</code>, and <code>guides/</code> as markdown with validated frontmatter. The process is five phases — preparation, proposal, implementation, review, completion — each encoded as a skill any competent coding agent can execute.</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-dark github-light" style="--shiki-dark:#e1e4e8;--shiki-light:#24292e;--shiki-dark-bg:#24292e;--shiki-light-bg:#fff;" tabindex="0" dir="ltr"><code><span class="line"><span>branch → draft PR → proposal → tests → guides → code → validation</span></span>
<span class="line"><span>       → cross-artifact review → adversarial review → preview → readiness report</span></span>
<span class="line"><span>       → human review → revision → vision update → merge → release → cleanup</span></span></code></pre></div><p>The parts that previously depended on the agent remembering become deterministic: a validator over the record, continuous integration running the same gates, and a git hook enforcing commit scoping — none of which depend on a harness noticing anything.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><p><strong>Artifacts.</strong> <code>VISION.md</code> at the root; <code>proposals/&lt;NNNN&gt;-&lt;slug&gt;.md</code>, <code>policies/&lt;NNNN&gt;-&lt;slug&gt;.md</code>, <code>decisions/&lt;NNNN&gt;-&lt;slug&gt;.md</code>, and <code>guides/&lt;slug&gt;.md</code>. Each carries YAML frontmatter with <code>id</code>, <code>title</code>, and <code>status</code>, plus kind-specific keys: <code>authors</code>, <code>pull-request</code>, <code>issues</code>, and <code>supersedes</code> on proposals; <code>established-by</code> and <code>supersedes</code> on policies and decisions; <code>describes</code> on guides. Templates in <code>.agents/templates/</code> are derived from the Swift Evolution templates, carrying instructional prose rather than bare section labels.</p><p><strong>Process skills.</strong> Seven under <code>.agents/skills/</code> — roughly one per phase, not one per step: <code>writing-a-proposal</code> (phase 2), <code>implementing-a-proposal</code> (phase 3: tests, guides, code), <code>reviewing-an-implementation</code> (phase 4: cross-artifact review, preview, readiness report), <code>adversarial-review</code> (phase 4, kept standalone because it is dispatched to a subagent with fresh context that reads only that file), <code>completing-a-feature</code> (phase 5, including recording policies and decisions), plus <code>tracking-defects</code> and <code>systematic-debugging</code> as cross-cutting.</p><p>A skill per stage was tried first and rejected. It recreated 1.0&#39;s central failure: a surface nobody could remember. Standing rules that always apply — verification before completion, code quality, commit discipline, the enforcement hierarchy — live in <code>.agents/rules/</code> instead, because a rule you invoke is a rule you forget to invoke.</p><p><strong>Orientation.</strong> <code>mise run status</code> derives the current phase from the branch, the pull request, and the active proposal&#39;s status, then prints what happens next and who acts. It takes no arguments. The human is never expected to remember a command, a skill name, or a step.</p><p><strong>Ordering.</strong> Implementation is strictly failing tests, then guides written from the proposal, then code. Guides are compared against the tests before code is written; any behavior in one and not the other is a defect in one of them.</p><p><strong>Validation.</strong> <code>tools/validate.mjs</code>, dependency-free Node, enforces five rules over the record: identifiers match filenames and are unique; every <code>supersedes</code>, <code>established-by</code>, and <code>describes</code> reference resolves; superseding an entry requires the superseded entry to be marked; required keys are present and <code>status</code> is within the set allowed for that kind; and no proposal past <code>draft</code> still contains an unresolved clarification marker, exempting the statuses where a design is legitimately being reworked or abandoned. Pure logic is separated from filesystem traversal so the rules are testable directly, without network access or third-party dependencies.</p><p><strong>Enforcement surfaces.</strong> <code>mise run check</code> runs formatting, validation, the validator&#39;s tests, and the guides build. <code>.github/workflows/ci.yml</code> runs the same gates plus commit-message validation over the pull request&#39;s commits. <code>.agents/hooks/commit-msg</code> is a POSIX git hook enforcing Scoped Commits, replacing 1.0&#39;s Claude-specific <code>PreToolUse</code> hook.</p><p><strong>Publication.</strong> The existing VitePress site is retained and retargeted from <code>specs/</code> and <code>features/</code> onto the record and the guides, with navigation ordered vision, decisions, policies, proposals, guides. Superseded entries move to a collapsed group rather than disappearing. An empty record directory yields an omitted or empty navigation group and must never fail the build, since a freshly adopted project has nothing in <code>policies/</code>, <code>decisions/</code>, or <code>guides/</code> yet.</p><p><strong>Harness neutrality.</strong> Everything ships in <code>.agents/</code> with <code>AGENTS.md</code> as the single contract. Adapting it for a specific harness is a documented one-time step.</p><p><strong>Guides.</strong> Workbench&#39;s own user-facing documentation lives in <code>guides/</code>, because a kit that treats guides as a load-bearing artifact and ships none teaches that the discipline is optional. The existing root documents serve different readers and none of them is a guide: <code>README.md</code> answers whether to use the kit, <code>PROCESS.md</code> states the process, and neither answers <em>how do I actually run a feature through this</em> — which is the only question a user has after the first hour. Without guides they would read <code>AGENTS.md</code>, a contract written for an agent, and infer their own role from it.</p><p>The set is task-oriented, one guide per question asked at a distinct moment: getting started, running a feature, writing a good proposal, choosing a preview, keeping the record, and adapting a harness. <code>running-a-feature</code> is the load-bearing one — it walks a single change from request to merge, showing what the human does, what <code>mise run status</code> reports at each step, and who moves the proposal&#39;s status when.</p><p>The harness adaptation guide lives at <code>guides/adapting-your-harness.md</code>; it is already a guide wearing a root-level filename, and the site publishes guides. Every worked example in a guide must be one that actually runs, per the guides discipline in <code>.agents/skills/implementing-a-proposal/</code>.</p><p><strong>Completeness of the cut.</strong> No surviving document may instruct the agent to avoid a branch, avoid pushing a feature branch, or avoid opening a pull request, and no reference to a platform, stack, or device may remain. Three separate 1.0 documents carried the first prohibition and the second is spread across nearly every file, so this is a property to verify rather than assume.</p><h2 id="compatibility" tabindex="-1">Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to “Compatibility”">​</a></h2><p>This is a breaking change for Workbench 1.0, and deliberately so. The two versions share almost nothing: 1.0&#39;s spec identifiers, reverse pointers, feature folders, and <code>/sdd-*</code> commands have no counterpart in 2.0. There is no migration path and none is offered.</p><p>Existing 1.0 users are unaffected until they choose to move, because a template repository is consumed by copying rather than by upgrading. 1.0 is preserved at the <code>v1.0</code> tag and remains usable through &quot;Use this template&quot; at that tag.</p><h2 id="implications-on-adoption" tabindex="-1">Implications on adoption <a class="header-anchor" href="#implications-on-adoption" aria-label="Permalink to “Implications on adoption”">​</a></h2><p>Adopting 2.0 requires <code>mise</code>, Node 26, and — for the pull-request phases — the <code>gh</code> CLI authenticated against the project&#39;s remote. A service account for posting readiness reports is recommended but optional; without one the agent posts from whatever identity is available and says so in the report.</p><p>The harness adaptation described in <code>guides/adapting-your-harness.md</code> is a one-time change that is committed to the project. It is reversible: deleting the generated harness configuration leaves <code>.agents/</code> intact.</p><p>Adopting the process is not all-or-nothing. A project can use the artifact structure without the pull-request phases, or the review stages without the validator. The gates are independent, and partial adoption degrades rather than breaks.</p><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to “Scope”">​</a></h2><ul><li>Delete the multiplatform apparatus: platform skills, device-driver skills, <code>/sdd-*</code> commands, subagents, per-platform task scaffolds, feature and spec templates, <code>specs/</code>, the Convex and Clerk environment contract, and the platform codegen hooks.</li><li>Write the agent contract, the process description, the adoption guide, and the vision.</li><li>Create the record directories, their templates, and their explanatory READMEs.</li><li>Rewrite the surviving process skills to proposal vocabulary and add the missing ones.</li><li>Rewrite commit discipline and the enforcement hierarchy; convert the commit hook to POSIX git.</li><li>Build the validator with tests; wire <code>mise</code> tasks, continuous integration, and the preview.</li><li>Retarget the documentation site.</li><li>Write the guides, including the harness adaptation guide, and point inbound links at them.</li></ul><h3 id="out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to “Out of scope”">​</a></h3><ul><li><strong>Slash commands.</strong> 1.0&#39;s <code>/sdd-*</code> surface is deleted and not replaced. Commands are harness-specific and the kit is harness-neutral; the skills carry the procedure instead.</li><li><strong>Dependent-project automation.</strong> <code>PROCESS.md</code> describes updating dependent repositories&#39; draft pull requests when a preview becomes a release. The skill documents the procedure; no tooling ships. Building it before a second repository exists would be speculative.</li><li><strong>Migration tooling for 1.0 users.</strong> See Compatibility.</li><li><strong>Preview mechanisms other than this repository&#39;s own.</strong> Recipes ship as comments. Wiring and testing all seven would mean maintaining six workflows nobody here runs.</li></ul><h2 id="preview" tabindex="-1">Preview <a class="header-anchor" href="#preview" aria-label="Permalink to “Preview”">​</a></h2><p>Workbench is a documentation and process repository, so its preview is the rendered site, deployed. <code>.github/workflows/preview.yml</code> builds it on every pull request, publishes it to GitHub Pages under <code>pr-preview/pr-&lt;n&gt;/</code>, comments the URL on the pull request, and removes both when the pull request closes.</p><p>A downloadable build artifact was tried first and rejected. It is not a preview: reviewing it means downloading a zip, unzipping it, and starting a local server, which nobody does. The whole point is that the human exercises the change, and friction at that step means the change gets reviewed as prose instead.</p><p>This repository is public, so its preview is public, which is correct — the guides and the record are already readable in the repository and rendering them exposes nothing new. Projects whose guides must stay private cannot use GitHub Pages, which serves privately only under Enterprise; the workflow carries a Cloudflare Pages recipe that can gate a preview behind Cloudflare Access.</p><p>The build honors a <code>DOCS_BASE</code> environment variable, because a site deployed to a subpath without a matching base renders every asset URL against the domain root and loads blank.</p><h2 id="policies-and-decisions-checked" tabindex="-1">Policies and decisions checked <a class="header-anchor" href="#policies-and-decisions-checked" aria-label="Permalink to “Policies and decisions checked”">​</a></h2><p><code>policies/</code> and <code>decisions/</code> are both empty. This is the first proposal in the repository, and 1.0 recorded neither as a first-class artifact — its <code>specs/CONVENTIONS.md</code> explicitly excluded decision logs, saying to &quot;use a <code>docs/</code> directory if you need one&quot;. Both directories were read; there is nothing to check against.</p><p>Two constraints were nonetheless inherited from 1.0 and deliberately preserved:</p><ul><li><code>.agents/rules/code-quality.md</code> — retained essentially unchanged. The validator&#39;s separation of pure logic from filesystem traversal is written to satisfy it.</li><li><code>.agents/rules/enforcement-hierarchy.md</code> — retained and retiered. The decision to put record invariants in a validator rather than in prose is an application of it.</li></ul><p>Neither is promoted to <code>policies/</code> here. They govern this repository rather than emerging from a feature, and speculative policies are the ones nobody follows.</p><h2 id="future-directions" tabindex="-1">Future directions <a class="header-anchor" href="#future-directions" aria-label="Permalink to “Future directions”">​</a></h2><ul><li>Mechanical checks beyond frontmatter: proposals with no corresponding tests, guides describing behavior no test covers, policies no recent proposal has cited.</li><li>Dependent-project automation, once a second repository consumes a Workbench preview.</li><li>Recording whether the adversarial stage catches defects the confirmatory stages miss, which would make it possible to evaluate the process rather than assume it.</li></ul><p>None of these are required for the kit to be usable, and each is large enough to deserve its own proposal.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to “Alternatives considered”">​</a></h2><p><strong>Keep the multiplatform apparatus as an optional layer, pruned at setup.</strong> This preserves real work and was the least disruptive option. Rejected because <code>PROCESS.md</code> mentions no platform anywhere, so the apparatus would be a second, undocumented half of the repository — which is precisely the condition that buried the process in 1.0. The <code>v1.0</code> tag preserves the work at zero maintenance cost.</p><p><strong>Split into two repositories: <code>workbench</code> and <code>workbench-platforms</code>.</strong> More respectful of the platform investment than deletion. Rejected because a live second repository carries ongoing maintenance, and a tag carries none while remaining fully usable.</p><p><strong>Ship configuration for all four harnesses named in <code>PROCESS.md</code>.</strong> Highest fidelity to the claim of harness-agnosticism. Rejected because only one can be genuinely verified here, and three untested configurations are worse than an honest adaptation step — they would fail quietly, in the harness the user actually chose. The cost is losing automatic lifecycle enforcement out of the box, which is why the commit hook and continuous integration were made harness-independent: the checks that matter most do not depend on the harness noticing.</p><p><strong>Keep 1.0&#39;s seven-file feature folder.</strong> The narrative, stories, use-cases, user flow, domain models, view models, and error definitions served cross-platform fan-out well. Rejected because with one implementation the taxonomy is overhead, and overhead on small changes is what causes a process to be skipped entirely.</p><p><strong>Publish only the guides, not the record.</strong> Simpler, and it sidesteps the question of whether a public documentation site should expose design history. Rejected because the record is the part of a project hardest to reconstruct later and hardest to navigate as raw files — publishing it turns cross-references into links and keeps superseded entries reachable. A project needing public guides and a private record can exclude the record directories in its own configuration, which is a narrower problem than making the record unreadable for everyone by default.</p><p><strong>A YAML dependency for the validator.</strong> Rejected as unnecessary. The frontmatter is flat scalars and inline lists; a reader that rejects what it does not understand is around thirty lines and keeps the validator runnable anywhere Node is.</p><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to “Open questions”">​</a></h2><p>None. Scope, harness strategy, preview model, defect tracking, the documentation surface, and the template lineage were all resolved with the human before implementation began.</p><h2 id="acknowledgments" tabindex="-1">Acknowledgments <a class="header-anchor" href="#acknowledgments" aria-label="Permalink to “Acknowledgments”">​</a></h2><p>The artifact templates are adapted from the <a href="https://github.com/swiftlang/swift-evolution" target="_blank" rel="noreferrer">Swift Evolution</a> proposal, policy, and vision documents. The instructional-prose approach — a template that teaches rather than labels — is theirs, and it is the single largest improvement to this proposal&#39;s design.</p><p>The review and test-first disciplines in <code>adversarial-review</code> and <code>implementing-a-proposal</code> originated in <a href="https://github.com/obra/superpowers" target="_blank" rel="noreferrer">Superpowers</a> and survives from Workbench 1.0.</p>`,66)])])}const m=t(i,[["render",r]]);export{u as __pageData,m as default};
